# Galaksay — ortam değişkenleri örneği.
# Gerçek değerler için .env veya .env.local oluşturun (bunlar git'e girmez).

# NuMap backend API kök adresi (öğretmen girişi + oturum okuma).
# Üretim:        https://getnumap.com/api
# Yerel NuMap dev: http://localhost:8788/api  (wrangler pages dev)
VITE_NUMAP_API=https://getnumap.com/api
